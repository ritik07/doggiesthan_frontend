import React from 'react'
import { AppController } from '../../appController/AppController'
import DogComp from '../../components/dog'

const Checkout = ({ history }) => {
  return (
    <AppController history={history}>
      <div>

      </div>
    </AppController>
      ) 
}

export default Checkout
